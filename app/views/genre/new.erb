<% @page_title = 'Add Genre' %>
<h2><%= @page_title %></h2>
<% form_for(:genre, :url => {:action => 'create'}) do |f| %>
    <%= render (:partial => 'form', :locals => {:f => f}) %>
    <%= submit_tag("Add Genre") %> | <%= link_to('Back', {:action => 'list'}, {:class => 'back-button'}) %>

<% end %>
