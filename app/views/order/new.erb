<% @page_title = 'Create Order' %>
<h2><%= @page_title %></h2>
<% form_for(:order, :url => {:action => 'create'}) do |f| %>
    <%= render (:partial => 'form', :locals => {:f => f}) %>
    <%= submit_tag("Create Order") %> | <%= link_to('Back', {:action => 'list'}, {:class => 'back-button'}) %>

<% end %>
