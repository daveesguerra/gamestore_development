<% @page_title = 'Add Publisher' %>
<h2><%= @page_title %></h2>
<% form_for(:publisher, :url => {:action => 'create'}) do |f| %>
    <%= render (:partial => 'form', :locals => {:f => f}) %>
    <%= submit_tag("Add Publisher") %> | <%= link_to('Back', {:action => 'list'}, {:class => 'back-button'}) %>

<% end %>
