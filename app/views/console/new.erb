<% @page_title = 'Add Console' %>
<h2>Add Console</h2>
<% form_for(:console, :url => {:action => 'create'}) do |f| %>
    <%= render (:partial => 'form', :locals => {:f => f}) %>
    <%= submit_tag("Add Console") %> | <%= link_to('Back', {:action => 'list'}, {:class => 'back-button'}) %>

<% end %>
